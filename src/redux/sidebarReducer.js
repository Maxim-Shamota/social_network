let  initialState = {
    friends: [
        {id: 1, name: 'Maxim'},
        {id: 2, name: 'Juliya'},
        {id: 3, name: 'Nataly'},
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer;